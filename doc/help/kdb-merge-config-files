# kdb-merge-config-files(1) -- Three-way merge of files with Elektra

## SYNOPSIS

`kdb merge-config-files <our> <their> <base> <format>`

`our`, `their` and `base` are the three files that should be merged.
`format` is the format of all the files.

## DESCRIPTION

Performs a three-way merge between three files using Elektra.
Takes into account the semantic properties of the files using `format`.
The result is written into the file `elektra-merge_result`.

## EXAMPLE

`kdb merge-config-files fileA fileB fileC ini`
