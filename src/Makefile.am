
# $Id$

# bin_PROGRAMS = localeinfo
# localeinfo_SOURCES = localeinfo.c

AM_CFLAGS=-g -O2

SUBDIRS = libloader backends libelektra libelektratools libregistry kdb include bindings
EXTRA_DIST = Doxyfile

