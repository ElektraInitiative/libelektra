list (FIND BINDINGS "intercept_fs" FINDEX)
if (FINDEX GREATER -1)
	message (STATUS "Include Binding intercept_fs")
	add_subdirectory (fs)
endif ()

list (FIND BINDINGS "intercept_env" FINDEX)
if (FINDEX GREATER -1)
	message (STATUS "Include Binding intercept_env")
	add_subdirectory (env)
endif ()
