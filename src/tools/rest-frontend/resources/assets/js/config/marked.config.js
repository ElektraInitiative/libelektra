(function() {

    'use strict';

    angular.module('elektra.rest.angular').config([
        'markedProvider',
        function(markedProvider) {

            markedProvider.setOptions({

			});

        }
    ]);

})();