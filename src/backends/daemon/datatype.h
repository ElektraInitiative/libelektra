typedef enum {
	DATATYPE_UNKNOW = 1,
	DATATYPE_INTEGER,
	DATATYPE_STRING,
	DATATYPE_KEY,			/* Complex data */
	DATATYPE_KEYSET,
	DATATYPE_MESSAGE,

	DATATYPE_LAST			/* Must be the last */
} DataType;


